import 'package:flutter/material.dart';

class VacancySearch extends StatelessWidget {
  const VacancySearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Vacancy Search Screen"),
      ),
    );
  }
}